import streamlit as st
import numpy as np
import matplotlib.pyplot as plt
import datetime

# Налаштування сторінки
st.set_page_config(page_title="Калькулятор тиску газу", layout="wide")

st.title("📊 Розрахунок параметрів тиску вибуху газу")
st.write("Сценарій: На території резервуарного парку складу стиснених газів відбулася розгерметизація резервуару із подальшим вибухом газу. Кількість речовини та її вид наведено в таблицях 1, 2. 
Завдання: Нанести радіуси уражень на план резервуару на місцевості. Для розуміння розмірів, діаметр резервуару становить 10 м. Провести розрахунок надлишкового тиску вибуху при викиді стисненого газу за заданих умов. Позначити радіуси меж критичних значень ∆P (100, 50, 30, 20, 5 кПа).
.")
st.write("Введіть параметри нижче, щоб оновити графік у реальному часі.")

# Створюємо бічну панель (Sidebar) для введення даних
with st.sidebar:
    st.header("Вхідні дані")
    m = st.number_input("Маса (m), кг", value=10.0, step=1.0)
    z = st.number_input("Коефіцієнт (z)", value=0.1, step=0.01, format="%.2f")
    Q_gas_val = st.number_input("Теплота згоряння речовини Q_gas (x10^6), Дж/кг", value=5.1, step=0.1)
    
    # Фізичні константи
    Q0 = 4.52 * 10**6
    P0 = 101.3

# Виконуємо розрахунки
Q_gas = Q_gas_val * 10**6
m_pr = (Q_gas * m * z) / Q0

# Відображення результату m_pr зверху
st.metric(label="Розраховане m_pr", value=f"{m_pr:.2f}")

# Дані для графіка
r = np.linspace(5, 200, 500)
# Уникаємо ділення на нуль, якщо r починається з 0
P = P0 * (0.8 * m_pr**0.33 / r + 3 * m_pr**0.66 / r**2 + 5 * m_pr / r**3)

# Створюємо дані для таблиці граничних значень
damage_data = {
    "Тиск (кПа)": ["2-5", "5-10", "10-20", "20-30", "30-50", "50-100", "> 100"],
    "Фізичні наслідки та руйнування": [
        "Дзвін скла, можливе розбиття вікон",
        "Помірні пошкодження віконних рам",
        "Тріщини в перегородках, руйнування шибок",
        "Часткове руйнування нежитлових будівель",
        "Руйнування цегляних стін, значні пошкодження дахів",
        "Повне руйнування стін будівель, загроза життю",
        "Повне руйнування споруд, летальні наслідки"
    ]
}

# Відображення заголовка та таблиці
st.subheader("⚠️ Граничні значення тиску та наслідки")
st.table(damage_data)

# Побудова графіка за допомогою Matplotlib
fig, ax = plt.subplots(figsize=(10, 5))
ax.plot(r, P, label=f"P(r) при m={m}", color='#1f77b4', lw=2.5)

# Додавання контрольних ліній (5, 20, 30, 50, 100)
for val in [5, 10, 20, 50, 100]:
    ax.axhline(y=val, color='red', linestyle='--', alpha=0.3)

ax.set_ylim(0, 110)
ax.set_xlim(0, 200)
ax.set_xlabel('Відстань r, м')
ax.set_ylabel('Тиск P, кПа')
ax.grid(True, which='both', linestyle=':', alpha=0.5)
ax.legend()

# Вивід графіка на вебсторінку
st.pyplot(fig)

# Отримуємо поточний рік автоматично
current_year = datetime.datetime.now().year

# Відображення авторських прав у футері
st.markdown("---") 
st.markdown(f"<p style='text-align: center; color: gray;'><small>© {current_year} Kostiantyn Afanasenko. Всі права захищені.</small></p>", unsafe_allow_html=True)

